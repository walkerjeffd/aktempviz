# reticulate::use_condaenv("aktempviz", required = TRUE)

# Sys.setenv(GOOGLE_APPLICATION_CREDENTIALS = "gee-service-account.json")

# rgee::ee_Initialize(
#   # user = 'gee-aktempviz@aktemp-walkerenvres.iam.gserviceaccount.com',
#   # drive = FALSE,
#   # gcs = FALSE,
#   auth_mode = "appdefault"
# )
